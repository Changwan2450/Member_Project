<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.member_project.mapper.OrderMapper">

    <insert id="insert">
        INSERT INTO ORDERS (fno, id, amount)
        VALUES (#{fno}, #{id}, #{amount})
    </insert>

    <select id="getList" resultType="com.kh.member_project.domain.Order">
        SELECT ono, fno, id, amount, regdate
        FROM ORDERS
        WHERE id = #{id}
        ORDER BY ono DESC
    </select>

    <select id="read" resultType="com.kh.member_project.domain.Order">
        SELECT ono, fno, id, amount, regdate
        FROM ORDERS
        WHERE ono = #{ono}
    </select>

    <delete id="delete">
        DELETE
        FROM ORDERS
        WHERE ono = #{ono}
    </delete>

</mapper>